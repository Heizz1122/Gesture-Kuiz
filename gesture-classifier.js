function classifyGesture(landmarks) {
  // Placeholder: kau boleh guna angle, distance, atau model
  // Contoh simple: return 0 untuk ✋, 1 untuk ✌️, 2 untuk ☝️, 3 untuk 🤟
  return Math.floor(Math.random() * 4); // Random untuk testing
}
